import './articlePart.css';
import { ArticleBlock } from './types/articlePart';

export const ArticlePart = ({title, subtitle, content, image_middle_src, image_first_src, image_second_src, image_middle_left_src} : ArticleBlock) => {
    return (
        <></>
    )
};